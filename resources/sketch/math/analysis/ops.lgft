import types.lgft

module analysis.ops

operation mappingOp {
    inputs = {
        math::variable x
    }
    outputs = {
        math::variable output
    }
}

operation sinFunc {
    inputs = { math::variable x }
    outputs = { math::variable output}
}

operation cosFunc{
    inputs = { math::variable x }
    outputs = { math::variable output}
}

operation expFunc{
    inputs = { math::variable x }
    outputs = { math::variable output}
}